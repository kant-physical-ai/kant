# 실습
[practice.py](practice.py)

# 체크리스트
- [x] `summary()` 가 모든 문항 통과로 나온다
- [x] `Kernel → Restart Kernel and Run All Cells` 로 처음부터 끝까지 오류 없이 통과했다
- [x] 커널이 **Python (pose_lab)** 으로 선택되어 있다
- [x] 난수를 쓴 곳은 `np.random.default_rng(시드)` 로 고정했다
- [x] **설명** 이라고 적힌 마크다운 셀을 모두 채웠다
- [x] 재사용할 코드는 `../src/` 의 `.py` 모듈로 내보냈다
- [x] `pip freeze > ../requirements.txt` 로 환경을 갱신했다